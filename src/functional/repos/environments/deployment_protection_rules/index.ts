import { IConnection, HttpError } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";

import { IApiReposEnvironmentsDeploymentProtectionRules } from "../../../../structures/IApiReposEnvironmentsDeploymentProtectionRules";
import { response } from "../../../../structures/response";
import { body } from "../../../../structures/body";
import { deployment_minus_protection_minus_rule } from "../../../../structures/deployment_minus_protection_minus_rule";
export * as apps from "./apps";
/**
 * Get all deployment protection rules for an environment.
 * Gets all custom deployment protection rules that are enabled for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
 *
 * For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app).
 *
 * OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
 *
 * @tag repos
 * @path repos/:owner/:repo/environments/:environment_name/deployment_protection_rules
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByOwnerAndRepoAndEnvironment_name(
  connection: IConnection<getByOwnerAndRepoAndEnvironment_name.Headers>,
  owner: string,
  repo: string,
  environment_name: string,
): Promise<getByOwnerAndRepoAndEnvironment_name.Output> {
  return !!connection.simulate
    ? getByOwnerAndRepoAndEnvironment_name.simulate(
        connection,
        owner,
        repo,
        environment_name,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByOwnerAndRepoAndEnvironment_name.METADATA,
          path: getByOwnerAndRepoAndEnvironment_name.path(
            owner,
            repo,
            environment_name,
          ),
          status: null,
        },
      );
}
export namespace getByOwnerAndRepoAndEnvironment_name {
  export type Headers =
    IApiReposEnvironmentsDeploymentProtectionRules.GetHeader;
  export type Output = response;

  export const METADATA = {
    method: "GET",
    path: "/repos/:owner/:repo/environments/:environment_name/deployment_protection_rules",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (owner: string, repo: string, environment_name: string) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/environments/${encodeURIComponent(environment_name ?? "null")}/deployment_protection_rules`;
  export const random = (g?: Partial<typia.IRandomGenerator>): response =>
    typia.random<response>(g);
  export const simulate = (
    connection: IConnection<getByOwnerAndRepoAndEnvironment_name.Headers>,
    owner: string,
    repo: string,
    environment_name: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByOwnerAndRepoAndEnvironment_name.path(
        owner,
        repo,
        environment_name,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("environment_name")(() => typia.assert(environment_name));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Create a custom deployment protection rule on an environment.
 * Enable a custom deployment protection rule for an environment.
 *
 * The authenticated user must have admin or owner permissions to the repository to use this endpoint.
 *
 * For more information about the app that is providing this custom deployment rule, see the [documentation for the `GET /apps/{app_slug}` endpoint](https://docs.github.com/rest/apps/apps#get-an-app).
 *
 * OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
 *
 * @tag repos
 * @path repos/:owner/:repo/environments/:environment_name/deployment_protection_rules
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOwnerAndRepoAndEnvironment_name(
  connection: IConnection<postByOwnerAndRepoAndEnvironment_name.Headers>,
  owner: string,
  repo: string,
  environment_name: string,
  body: postByOwnerAndRepoAndEnvironment_name.Input,
): Promise<postByOwnerAndRepoAndEnvironment_name.Output> {
  return !!connection.simulate
    ? postByOwnerAndRepoAndEnvironment_name.simulate(
        connection,
        owner,
        repo,
        environment_name,
        body,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOwnerAndRepoAndEnvironment_name.METADATA,
          path: postByOwnerAndRepoAndEnvironment_name.path(
            owner,
            repo,
            environment_name,
          ),
          status: null,
        },
        body,
      );
}
export namespace postByOwnerAndRepoAndEnvironment_name {
  export type Headers =
    IApiReposEnvironmentsDeploymentProtectionRules.PostHeader;
  export type Input = body;
  export type Output = deployment_minus_protection_minus_rule;

  export const METADATA = {
    method: "POST",
    path: "/repos/:owner/:repo/environments/:environment_name/deployment_protection_rules",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (owner: string, repo: string, environment_name: string) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/environments/${encodeURIComponent(environment_name ?? "null")}/deployment_protection_rules`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): deployment_minus_protection_minus_rule =>
    typia.random<deployment_minus_protection_minus_rule>(g);
  export const simulate = (
    connection: IConnection<postByOwnerAndRepoAndEnvironment_name.Headers>,
    owner: string,
    repo: string,
    environment_name: string,
    body: postByOwnerAndRepoAndEnvironment_name.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOwnerAndRepoAndEnvironment_name.path(
        owner,
        repo,
        environment_name,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("environment_name")(() => typia.assert(environment_name));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Get a custom deployment protection rule.
 * Gets an enabled custom deployment protection rule for an environment. Anyone with read access to the repository can use this endpoint. For more information about environments, see "[Using environments for deployment](https://docs.github.com/actions/deployment/targeting-different-environments/using-environments-for-deployment)."
 *
 * For more information about the app that is providing this custom deployment rule, see [`GET /apps/{app_slug}`](https://docs.github.com/rest/apps/apps#get-an-app).
 *
 * OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint with a private repository.
 *
 * @tag repos
 * @path repos/:owner/:repo/environments/:environment_name/deployment_protection_rules/:protection_rule_id
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id(
  connection: IConnection<getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.Headers>,
  owner: string,
  repo: string,
  environment_name: string,
  protection_rule_id: string,
): Promise<getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.Output> {
  return !!connection.simulate
    ? getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.simulate(
        connection,
        owner,
        repo,
        environment_name,
        protection_rule_id,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.METADATA,
          path: getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.path(
            owner,
            repo,
            environment_name,
            protection_rule_id,
          ),
          status: null,
        },
      );
}
export namespace getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id {
  export type Headers =
    IApiReposEnvironmentsDeploymentProtectionRules.GetHeader;
  export type Output = deployment_minus_protection_minus_rule;

  export const METADATA = {
    method: "GET",
    path: "/repos/:owner/:repo/environments/:environment_name/deployment_protection_rules/:protection_rule_id",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    owner: string,
    repo: string,
    environment_name: string,
    protection_rule_id: string,
  ) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/environments/${encodeURIComponent(environment_name ?? "null")}/deployment_protection_rules/${encodeURIComponent(protection_rule_id ?? "null")}`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): deployment_minus_protection_minus_rule =>
    typia.random<deployment_minus_protection_minus_rule>(g);
  export const simulate = (
    connection: IConnection<getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.Headers>,
    owner: string,
    repo: string,
    environment_name: string,
    protection_rule_id: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.path(
        owner,
        repo,
        environment_name,
        protection_rule_id,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("environment_name")(() => typia.assert(environment_name));
      assert.param("protection_rule_id")(() =>
        typia.assert(protection_rule_id),
      );
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Disable a custom protection rule for an environment.
 * Disables a custom deployment protection rule for an environment.
 *
 * The authenticated user must have admin or owner permissions to the repository to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need the `repo` scope to use this endpoint.
 *
 * @tag repos
 * @path repos/:owner/:repo/environments/:environment_name/deployment_protection_rules/:protection_rule_id
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id(
  connection: IConnection<eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.Headers>,
  owner: string,
  repo: string,
  environment_name: string,
  protection_rule_id: string,
): Promise<void> {
  return !!connection.simulate
    ? eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.simulate(
        connection,
        owner,
        repo,
        environment_name,
        protection_rule_id,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.METADATA,
          path: eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.path(
            owner,
            repo,
            environment_name,
            protection_rule_id,
          ),
          status: null,
        },
      );
}
export namespace eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id {
  export type Headers =
    IApiReposEnvironmentsDeploymentProtectionRules.DeleteHeader;

  export const METADATA = {
    method: "DELETE",
    path: "/repos/:owner/:repo/environments/:environment_name/deployment_protection_rules/:protection_rule_id",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    owner: string,
    repo: string,
    environment_name: string,
    protection_rule_id: string,
  ) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/environments/${encodeURIComponent(environment_name ?? "null")}/deployment_protection_rules/${encodeURIComponent(protection_rule_id ?? "null")}`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.Headers>,
    owner: string,
    repo: string,
    environment_name: string,
    protection_rule_id: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: eraseByOwnerAndRepoAndEnvironment_nameAndProtection_rule_id.path(
        owner,
        repo,
        environment_name,
        protection_rule_id,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("environment_name")(() => typia.assert(environment_name));
      assert.param("protection_rule_id")(() =>
        typia.assert(protection_rule_id),
      );
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
