import { IConnection, HttpError } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";

import { IApiReposSecurityAdvisoriesForks } from "../../../../structures/IApiReposSecurityAdvisoriesForks";
/**
 * Create a temporary private fork to collaborate on fixing a security vulnerability in your repository.
 *
 * > [!NOTE]
 * > Forking a repository happens asynchronously. You may have to wait up to 5 minutes before you can access the fork.
 *
 * @tag security-advisories
 * @path repos/:owner/:repo/security-advisories/:ghsa_id/forks
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOwnerAndRepoAndGhsa_id(
  connection: IConnection<postByOwnerAndRepoAndGhsa_id.Headers>,
  owner: string,
  repo: string,
  ghsa_id: string,
): Promise<void> {
  return !!connection.simulate
    ? postByOwnerAndRepoAndGhsa_id.simulate(connection, owner, repo, ghsa_id)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOwnerAndRepoAndGhsa_id.METADATA,
          path: postByOwnerAndRepoAndGhsa_id.path(owner, repo, ghsa_id),
          status: null,
        },
      );
}
export namespace postByOwnerAndRepoAndGhsa_id {
  export type Headers = IApiReposSecurityAdvisoriesForks.PostHeader;

  export const METADATA = {
    method: "POST",
    path: "/repos/:owner/:repo/security-advisories/:ghsa_id/forks",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (owner: string, repo: string, ghsa_id: string) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/security-advisories/${encodeURIComponent(ghsa_id ?? "null")}/forks`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<postByOwnerAndRepoAndGhsa_id.Headers>,
    owner: string,
    repo: string,
    ghsa_id: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOwnerAndRepoAndGhsa_id.path(owner, repo, ghsa_id),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("ghsa_id")(() => typia.assert(ghsa_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
