import { IConnection, HttpError } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";

import { IApiReposHooksDeliveriesAttempts } from "../../../../../structures/IApiReposHooksDeliveriesAttempts";
/**
 * Redeliver a delivery for a repository webhook.
 * Redeliver a webhook delivery for a webhook configured in a repository.
 *
 * @tag repos
 * @path repos/:owner/:repo/hooks/:hook_id/deliveries/:delivery_id/attempts
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOwnerAndRepoAndHook_idAndDelivery_id(
  connection: IConnection<postByOwnerAndRepoAndHook_idAndDelivery_id.Headers>,
  owner: string,
  repo: string,
  hook_id: string,
  delivery_id: string,
): Promise<void> {
  return !!connection.simulate
    ? postByOwnerAndRepoAndHook_idAndDelivery_id.simulate(
        connection,
        owner,
        repo,
        hook_id,
        delivery_id,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOwnerAndRepoAndHook_idAndDelivery_id.METADATA,
          path: postByOwnerAndRepoAndHook_idAndDelivery_id.path(
            owner,
            repo,
            hook_id,
            delivery_id,
          ),
          status: null,
        },
      );
}
export namespace postByOwnerAndRepoAndHook_idAndDelivery_id {
  export type Headers = IApiReposHooksDeliveriesAttempts.PostHeader;

  export const METADATA = {
    method: "POST",
    path: "/repos/:owner/:repo/hooks/:hook_id/deliveries/:delivery_id/attempts",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    owner: string,
    repo: string,
    hook_id: string,
    delivery_id: string,
  ) =>
    `/repos/${encodeURIComponent(owner ?? "null")}/${encodeURIComponent(repo ?? "null")}/hooks/${encodeURIComponent(hook_id ?? "null")}/deliveries/${encodeURIComponent(delivery_id ?? "null")}/attempts`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<postByOwnerAndRepoAndHook_idAndDelivery_id.Headers>,
    owner: string,
    repo: string,
    hook_id: string,
    delivery_id: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOwnerAndRepoAndHook_idAndDelivery_id.path(
        owner,
        repo,
        hook_id,
        delivery_id,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("owner")(() => typia.assert(owner));
      assert.param("repo")(() => typia.assert(repo));
      assert.param("hook_id")(() => typia.assert(hook_id));
      assert.param("delivery_id")(() => typia.assert(delivery_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
