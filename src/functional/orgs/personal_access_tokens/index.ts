import { HttpError, IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import { IApiOrgsPersonalAccessTokens } from "../../../structures/IApiOrgsPersonalAccessTokens";
import { body } from "../../../structures/body";
import { organization_minus_programmatic_minus_access_minus_grant } from "../../../structures/organization_minus_programmatic_minus_access_minus_grant";

export * as repositories from "./repositories";
/**
 * List fine-grained personal access tokens with access to organization resources.
 * Lists approved fine-grained personal access tokens owned by organization members that can access organization resources.
 *
 * Only GitHub Apps can use this endpoint.
 *
 * @tag orgs
 * @path orgs/:org/personal-access-tokens
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByOrg(
  connection: IConnection<getByOrg.Headers>,
  org: string,
): Promise<getByOrg.Output> {
  return !!connection.simulate
    ? getByOrg.simulate(connection, org)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByOrg.METADATA,
          path: getByOrg.path(org),
          status: null,
        },
      );
}
export namespace getByOrg {
  export type Headers = IApiOrgsPersonalAccessTokens.GetHeader;
  export type Output =
    organization_minus_programmatic_minus_access_minus_grant[];

  export const METADATA = {
    method: "GET",
    path: "/orgs/:org/personal-access-tokens",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (org: string) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/personal-access-tokens`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): organization_minus_programmatic_minus_access_minus_grant[] =>
    typia.random<organization_minus_programmatic_minus_access_minus_grant[]>(g);
  export const simulate = (
    connection: IConnection<getByOrg.Headers>,
    org: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByOrg.path(org),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Update the access to organization resources via fine-grained personal access tokens.
 * Updates the access organization members have to organization resources via fine-grained personal access tokens. Limited to revoking a token's existing access.
 *
 * Only GitHub Apps can use this endpoint.
 *
 * @tag orgs
 * @path orgs/:org/personal-access-tokens
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOrg(
  connection: IConnection<postByOrg.Headers>,
  org: string,
  body: postByOrg.Input,
): Promise<void> {
  return !!connection.simulate
    ? postByOrg.simulate(connection, org, body)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOrg.METADATA,
          path: postByOrg.path(org),
          status: null,
        },
        body,
      );
}
export namespace postByOrg {
  export type Headers = IApiOrgsPersonalAccessTokens.PostHeader;
  export type Input = body;

  export const METADATA = {
    method: "POST",
    path: "/orgs/:org/personal-access-tokens",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (org: string) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/personal-access-tokens`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<postByOrg.Headers>,
    org: string,
    body: postByOrg.Input,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOrg.path(org),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Update the access a fine-grained personal access token has to organization resources.
 * Updates the access an organization member has to organization resources via a fine-grained personal access token. Limited to revoking the token's existing access. Limited to revoking a token's existing access.
 *
 * Only GitHub Apps can use this endpoint.
 *
 * @tag orgs
 * @path orgs/:org/personal-access-tokens/:pat_id
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOrgAndPat_id(
  connection: IConnection<postByOrgAndPat_id.Headers>,
  org: string,
  pat_id: string,
  body: postByOrgAndPat_id.Input,
): Promise<void> {
  return !!connection.simulate
    ? postByOrgAndPat_id.simulate(connection, org, pat_id, body)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOrgAndPat_id.METADATA,
          path: postByOrgAndPat_id.path(org, pat_id),
          status: null,
        },
        body,
      );
}
export namespace postByOrgAndPat_id {
  export type Headers = IApiOrgsPersonalAccessTokens.PostHeader;
  export type Input = body;

  export const METADATA = {
    method: "POST",
    path: "/orgs/:org/personal-access-tokens/:pat_id",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (org: string, pat_id: string) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/personal-access-tokens/${encodeURIComponent(pat_id ?? "null")}`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<postByOrgAndPat_id.Headers>,
    org: string,
    pat_id: string,
    body: postByOrgAndPat_id.Input,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOrgAndPat_id.path(org, pat_id),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
      assert.param("pat_id")(() => typia.assert(pat_id));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
