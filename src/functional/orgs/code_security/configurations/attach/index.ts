import { HttpError, IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import { IApiOrgsCodeSecurityConfigurationsAttach } from "../../../../../structures/IApiOrgsCodeSecurityConfigurationsAttach";
import { body } from "../../../../../structures/body";

/**
 * Attach a configuration to repositories.
 * Attach a code security configuration to a set of repositories. If the repositories specified are already attached to a configuration, they will be re-attached to the provided configuration.
 *
 * If insufficient GHAS licenses are available to attach the configuration to a repository, only free features will be enabled.
 *
 * The authenticated user must be an administrator or security manager for the organization to use this endpoint.
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:org` scope to use this endpoint.
 *
 * @tag code-security
 * @path orgs/:org/code-security/configurations/:configuration_id/attach
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByOrgAndConfiguration_id(
  connection: IConnection<postByOrgAndConfiguration_id.Headers>,
  org: string,
  configuration_id: string,
  body: postByOrgAndConfiguration_id.Input,
): Promise<void> {
  return !!connection.simulate
    ? postByOrgAndConfiguration_id.simulate(
        connection,
        org,
        configuration_id,
        body,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByOrgAndConfiguration_id.METADATA,
          path: postByOrgAndConfiguration_id.path(org, configuration_id),
          status: null,
        },
        body,
      );
}
export namespace postByOrgAndConfiguration_id {
  export type Headers = IApiOrgsCodeSecurityConfigurationsAttach.PostHeader;
  export type Input = body;

  export const METADATA = {
    method: "POST",
    path: "/orgs/:org/code-security/configurations/:configuration_id/attach",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (org: string, configuration_id: string) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/code-security/configurations/${encodeURIComponent(configuration_id ?? "null")}/attach`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<postByOrgAndConfiguration_id.Headers>,
    org: string,
    configuration_id: string,
    body: postByOrgAndConfiguration_id.Input,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByOrgAndConfiguration_id.path(org, configuration_id),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
      assert.param("configuration_id")(() => typia.assert(configuration_id));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
