import { IConnection, HttpError } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";

import { IApiOrgsMigrationsReposLock } from "../../../../../structures/IApiOrgsMigrationsReposLock";
/**
 * Unlock an organization repository.
 * Unlocks a repository that was locked for migration. You should unlock each migrated repository and [delete them](https://docs.github.com/rest/repos/repos#delete-a-repository) when the migration is complete and you no longer need the source data.
 *
 * @tag migrations
 * @path orgs/:org/migrations/:migration_id/repos/:repo_name/lock
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function eraseByOrgAndMigration_idAndRepo_name(
  connection: IConnection<eraseByOrgAndMigration_idAndRepo_name.Headers>,
  org: string,
  migration_id: string,
  repo_name: string,
): Promise<void> {
  return !!connection.simulate
    ? eraseByOrgAndMigration_idAndRepo_name.simulate(
        connection,
        org,
        migration_id,
        repo_name,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...eraseByOrgAndMigration_idAndRepo_name.METADATA,
          path: eraseByOrgAndMigration_idAndRepo_name.path(
            org,
            migration_id,
            repo_name,
          ),
          status: null,
        },
      );
}
export namespace eraseByOrgAndMigration_idAndRepo_name {
  export type Headers = IApiOrgsMigrationsReposLock.DeleteHeader;

  export const METADATA = {
    method: "DELETE",
    path: "/orgs/:org/migrations/:migration_id/repos/:repo_name/lock",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (org: string, migration_id: string, repo_name: string) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/migrations/${encodeURIComponent(migration_id ?? "null")}/repos/${encodeURIComponent(repo_name ?? "null")}/lock`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<eraseByOrgAndMigration_idAndRepo_name.Headers>,
    org: string,
    migration_id: string,
    repo_name: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: eraseByOrgAndMigration_idAndRepo_name.path(
        org,
        migration_id,
        repo_name,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
      assert.param("migration_id")(() => typia.assert(migration_id));
      assert.param("repo_name")(() => typia.assert(repo_name));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
