import { HttpError, IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia, { tags } from "typia";

import { IApiOrgsPersonalAccessTokenRequestsRepositories } from "../../../../structures/IApiOrgsPersonalAccessTokenRequestsRepositories";
import { minimal_minus_repository } from "../../../../structures/minimal_minus_repository";

/**
 * List repositories requested to be accessed by a fine-grained personal access token.
 * Lists the repositories a fine-grained personal access token request is requesting access to.
 *
 * Only GitHub Apps can use this endpoint.
 *
 * @param pat_request_id Unique identifier of the request for access via fine-grained personal access token.
 * @tag orgs
 * @path orgs/:org/personal-access-token-requests/:pat_request_id/repositories
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByOrgAndPat_request_id(
  connection: IConnection<getByOrgAndPat_request_id.Headers>,
  org: string,
  pat_request_id: number & tags.Type<"int32">,
): Promise<getByOrgAndPat_request_id.Output> {
  return !!connection.simulate
    ? getByOrgAndPat_request_id.simulate(connection, org, pat_request_id)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByOrgAndPat_request_id.METADATA,
          path: getByOrgAndPat_request_id.path(org, pat_request_id),
          status: null,
        },
      );
}
export namespace getByOrgAndPat_request_id {
  export type Headers =
    IApiOrgsPersonalAccessTokenRequestsRepositories.GetHeader;
  export type Output = minimal_minus_repository[];

  export const METADATA = {
    method: "GET",
    path: "/orgs/:org/personal-access-token-requests/:pat_request_id/repositories",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    org: string,
    pat_request_id: number & tags.Type<"int32">,
  ) =>
    `/orgs/${encodeURIComponent(org ?? "null")}/personal-access-token-requests/${encodeURIComponent(pat_request_id ?? "null")}/repositories`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): minimal_minus_repository[] => typia.random<minimal_minus_repository[]>(g);
  export const simulate = (
    connection: IConnection<getByOrgAndPat_request_id.Headers>,
    org: string,
    pat_request_id: number & tags.Type<"int32">,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByOrgAndPat_request_id.path(org, pat_request_id),
      contentType: "application/json",
    });
    try {
      assert.param("org")(() => typia.assert(org));
      assert.param("pat_request_id")(() => typia.assert(pat_request_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
