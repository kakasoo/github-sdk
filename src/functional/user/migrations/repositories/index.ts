import { HttpError, IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import { IApiUserMigrationsRepositories } from "../../../../structures/IApiUserMigrationsRepositories";
import { minimal_minus_repository } from "../../../../structures/minimal_minus_repository";

/**
 * List repositories for a user migration.
 * Lists all the repositories for this user migration.
 *
 * @tag migrations
 * @path user/migrations/:migration_id/repositories
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByMigration_id(
  connection: IConnection<getByMigration_id.Headers>,
  migration_id: string,
): Promise<getByMigration_id.Output> {
  return !!connection.simulate
    ? getByMigration_id.simulate(connection, migration_id)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByMigration_id.METADATA,
          path: getByMigration_id.path(migration_id),
          status: null,
        },
      );
}
export namespace getByMigration_id {
  export type Headers = IApiUserMigrationsRepositories.GetHeader;
  export type Output = minimal_minus_repository[];

  export const METADATA = {
    method: "GET",
    path: "/user/migrations/:migration_id/repositories",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (migration_id: string) =>
    `/user/migrations/${encodeURIComponent(migration_id ?? "null")}/repositories`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): minimal_minus_repository[] => typia.random<minimal_minus_repository[]>(g);
  export const simulate = (
    connection: IConnection<getByMigration_id.Headers>,
    migration_id: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByMigration_id.path(migration_id),
      contentType: "application/json",
    });
    try {
      assert.param("migration_id")(() => typia.assert(migration_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
