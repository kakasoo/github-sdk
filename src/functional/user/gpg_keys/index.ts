import { IConnection, HttpError } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";

import { IApiUserGpgKeys } from "../../../structures/IApiUserGpgKeys";
import { gpg_minus_key } from "../../../structures/gpg_minus_key";
import { body } from "../../../structures/body";
/**
 * List GPG keys for the authenticated user.
 * Lists the current user's GPG keys.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
 *
 * @tag users
 * @path user/gpg_keys
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function get(
  connection: IConnection<get.Headers>,
): Promise<get.Output> {
  return !!connection.simulate
    ? get.simulate(connection)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...get.METADATA,
          path: get.path(),
          status: null,
        },
      );
}
export namespace get {
  export type Headers = IApiUserGpgKeys.GetHeader;
  export type Output = gpg_minus_key[];

  export const METADATA = {
    method: "GET",
    path: "/user/gpg_keys",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = () => "/user/gpg_keys";
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): gpg_minus_key[] => typia.random<gpg_minus_key[]>(g);
  export const simulate = (connection: IConnection<get.Headers>): Output => {
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Create a GPG key for the authenticated user.
 * Adds a GPG key to the authenticated user's GitHub account.
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:gpg_key` scope to use this endpoint.
 *
 * @tag users
 * @path user/gpg_keys
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function post(
  connection: IConnection,
  body: post.Input,
): Promise<post.Output> {
  return !!connection.simulate
    ? post.simulate(connection, body)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...post.METADATA,
          path: post.path(),
          status: null,
        },
        body,
      );
}
export namespace post {
  export type Input = body;
  export type Output = gpg_minus_key;

  export const METADATA = {
    method: "POST",
    path: "/user/gpg_keys",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = () => "/user/gpg_keys";
  export const random = (g?: Partial<typia.IRandomGenerator>): gpg_minus_key =>
    typia.random<gpg_minus_key>(g);
  export const simulate = (
    connection: IConnection,
    body: post.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: post.path(),
      contentType: "application/json",
    });
    try {
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Get a GPG key for the authenticated user.
 * View extended details for a single GPG key.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:gpg_key` scope to use this endpoint.
 *
 * @tag users
 * @path user/gpg_keys/:gpg_key_id
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByGpg_key_id(
  connection: IConnection<getByGpg_key_id.Headers>,
  gpg_key_id: string,
): Promise<getByGpg_key_id.Output> {
  return !!connection.simulate
    ? getByGpg_key_id.simulate(connection, gpg_key_id)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByGpg_key_id.METADATA,
          path: getByGpg_key_id.path(gpg_key_id),
          status: null,
        },
      );
}
export namespace getByGpg_key_id {
  export type Headers = IApiUserGpgKeys.GetHeader;
  export type Output = gpg_minus_key;

  export const METADATA = {
    method: "GET",
    path: "/user/gpg_keys/:gpg_key_id",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (gpg_key_id: string) =>
    `/user/gpg_keys/${encodeURIComponent(gpg_key_id ?? "null")}`;
  export const random = (g?: Partial<typia.IRandomGenerator>): gpg_minus_key =>
    typia.random<gpg_minus_key>(g);
  export const simulate = (
    connection: IConnection<getByGpg_key_id.Headers>,
    gpg_key_id: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByGpg_key_id.path(gpg_key_id),
      contentType: "application/json",
    });
    try {
      assert.param("gpg_key_id")(() => typia.assert(gpg_key_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Delete a GPG key for the authenticated user.
 * Removes a GPG key from the authenticated user's GitHub account.
 *
 * OAuth app tokens and personal access tokens (classic) need the `admin:gpg_key` scope to use this endpoint.
 *
 * @tag users
 * @path user/gpg_keys/:gpg_key_id
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function eraseByGpg_key_id(
  connection: IConnection<eraseByGpg_key_id.Headers>,
  gpg_key_id: string,
): Promise<void> {
  return !!connection.simulate
    ? eraseByGpg_key_id.simulate(connection, gpg_key_id)
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...eraseByGpg_key_id.METADATA,
          path: eraseByGpg_key_id.path(gpg_key_id),
          status: null,
        },
      );
}
export namespace eraseByGpg_key_id {
  export type Headers = IApiUserGpgKeys.DeleteHeader;

  export const METADATA = {
    method: "DELETE",
    path: "/user/gpg_keys/:gpg_key_id",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (gpg_key_id: string) =>
    `/user/gpg_keys/${encodeURIComponent(gpg_key_id ?? "null")}`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<eraseByGpg_key_id.Headers>,
    gpg_key_id: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: eraseByGpg_key_id.path(gpg_key_id),
      contentType: "application/json",
    });
    try {
      assert.param("gpg_key_id")(() => typia.assert(gpg_key_id));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
