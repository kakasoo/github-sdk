import { HttpError, IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import { IApiTeamsDiscussionsComments } from "../../../../structures/IApiTeamsDiscussionsComments";
import { body } from "../../../../structures/body";
import { team_minus_discussion_minus_comment } from "../../../../structures/team_minus_discussion_minus_comment";

export * as reactions from "./reactions";
/**
 * List discussion comments (Legacy).
 * > [!WARNING]
 * > **Deprecation notice:** This endpoint route is deprecated and will be removed from the Teams API. We recommend migrating your existing code to use the new [List discussion comments](https://docs.github.com/rest/teams/discussion-comments#list-discussion-comments) endpoint.
 *
 * List all comments on a team discussion.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:discussion` scope to use this endpoint.
 *
 * @tag teams
 * @deprecated
 * @path teams/:team_id/discussions/:discussion_number/comments
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByTeam_idAndDiscussion_number(
  connection: IConnection<getByTeam_idAndDiscussion_number.Headers>,
  team_id: string,
  discussion_number: string,
): Promise<getByTeam_idAndDiscussion_number.Output> {
  return !!connection.simulate
    ? getByTeam_idAndDiscussion_number.simulate(
        connection,
        team_id,
        discussion_number,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByTeam_idAndDiscussion_number.METADATA,
          path: getByTeam_idAndDiscussion_number.path(
            team_id,
            discussion_number,
          ),
          status: null,
        },
      );
}
export namespace getByTeam_idAndDiscussion_number {
  export type Headers = IApiTeamsDiscussionsComments.GetHeader;
  export type Output = team_minus_discussion_minus_comment[];

  export const METADATA = {
    method: "GET",
    path: "/teams/:team_id/discussions/:discussion_number/comments",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (team_id: string, discussion_number: string) =>
    `/teams/${encodeURIComponent(team_id ?? "null")}/discussions/${encodeURIComponent(discussion_number ?? "null")}/comments`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): team_minus_discussion_minus_comment[] =>
    typia.random<team_minus_discussion_minus_comment[]>(g);
  export const simulate = (
    connection: IConnection<getByTeam_idAndDiscussion_number.Headers>,
    team_id: string,
    discussion_number: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByTeam_idAndDiscussion_number.path(team_id, discussion_number),
      contentType: "application/json",
    });
    try {
      assert.param("team_id")(() => typia.assert(team_id));
      assert.param("discussion_number")(() => typia.assert(discussion_number));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Create a discussion comment (Legacy).
 * > [!WARNING]
 * > **Deprecation notice:** This endpoint route is deprecated and will be removed from the Teams API. We recommend migrating your existing code to use the new [Create a discussion comment](https://docs.github.com/rest/teams/discussion-comments#create-a-discussion-comment) endpoint.
 *
 * Creates a new comment on a team discussion.
 *
 * This endpoint triggers [notifications](https://docs.github.com/github/managing-subscriptions-and-notifications-on-github/about-notifications). Creating content too quickly using this endpoint may result in secondary rate limiting. For more information, see "[Rate limits for the API](https://docs.github.com/rest/using-the-rest-api/rate-limits-for-the-rest-api#about-secondary-rate-limits)" and "[Best practices for using the REST API](https://docs.github.com/rest/guides/best-practices-for-using-the-rest-api)."
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:discussion` scope to use this endpoint.
 *
 * @tag teams
 * @deprecated
 * @path teams/:team_id/discussions/:discussion_number/comments
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function postByTeam_idAndDiscussion_number(
  connection: IConnection<postByTeam_idAndDiscussion_number.Headers>,
  team_id: string,
  discussion_number: string,
  body: postByTeam_idAndDiscussion_number.Input,
): Promise<postByTeam_idAndDiscussion_number.Output> {
  return !!connection.simulate
    ? postByTeam_idAndDiscussion_number.simulate(
        connection,
        team_id,
        discussion_number,
        body,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...postByTeam_idAndDiscussion_number.METADATA,
          path: postByTeam_idAndDiscussion_number.path(
            team_id,
            discussion_number,
          ),
          status: null,
        },
        body,
      );
}
export namespace postByTeam_idAndDiscussion_number {
  export type Headers = IApiTeamsDiscussionsComments.PostHeader;
  export type Input = body;
  export type Output = team_minus_discussion_minus_comment;

  export const METADATA = {
    method: "POST",
    path: "/teams/:team_id/discussions/:discussion_number/comments",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (team_id: string, discussion_number: string) =>
    `/teams/${encodeURIComponent(team_id ?? "null")}/discussions/${encodeURIComponent(discussion_number ?? "null")}/comments`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): team_minus_discussion_minus_comment =>
    typia.random<team_minus_discussion_minus_comment>(g);
  export const simulate = (
    connection: IConnection<postByTeam_idAndDiscussion_number.Headers>,
    team_id: string,
    discussion_number: string,
    body: postByTeam_idAndDiscussion_number.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: postByTeam_idAndDiscussion_number.path(team_id, discussion_number),
      contentType: "application/json",
    });
    try {
      assert.param("team_id")(() => typia.assert(team_id));
      assert.param("discussion_number")(() => typia.assert(discussion_number));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Get a discussion comment (Legacy).
 * > [!WARNING]
 * > **Deprecation notice:** This endpoint route is deprecated and will be removed from the Teams API. We recommend migrating your existing code to use the new [Get a discussion comment](https://docs.github.com/rest/teams/discussion-comments#get-a-discussion-comment) endpoint.
 *
 * Get a specific comment on a team discussion.
 *
 * OAuth app tokens and personal access tokens (classic) need the `read:discussion` scope to use this endpoint.
 *
 * @tag teams
 * @deprecated
 * @path teams/:team_id/discussions/:discussion_number/comments/:comment_number
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getByTeam_idAndDiscussion_numberAndComment_number(
  connection: IConnection<getByTeam_idAndDiscussion_numberAndComment_number.Headers>,
  team_id: string,
  discussion_number: string,
  comment_number: string,
): Promise<getByTeam_idAndDiscussion_numberAndComment_number.Output> {
  return !!connection.simulate
    ? getByTeam_idAndDiscussion_numberAndComment_number.simulate(
        connection,
        team_id,
        discussion_number,
        comment_number,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...getByTeam_idAndDiscussion_numberAndComment_number.METADATA,
          path: getByTeam_idAndDiscussion_numberAndComment_number.path(
            team_id,
            discussion_number,
            comment_number,
          ),
          status: null,
        },
      );
}
export namespace getByTeam_idAndDiscussion_numberAndComment_number {
  export type Headers = IApiTeamsDiscussionsComments.GetHeader;
  export type Output = team_minus_discussion_minus_comment;

  export const METADATA = {
    method: "GET",
    path: "/teams/:team_id/discussions/:discussion_number/comments/:comment_number",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    team_id: string,
    discussion_number: string,
    comment_number: string,
  ) =>
    `/teams/${encodeURIComponent(team_id ?? "null")}/discussions/${encodeURIComponent(discussion_number ?? "null")}/comments/${encodeURIComponent(comment_number ?? "null")}`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): team_minus_discussion_minus_comment =>
    typia.random<team_minus_discussion_minus_comment>(g);
  export const simulate = (
    connection: IConnection<getByTeam_idAndDiscussion_numberAndComment_number.Headers>,
    team_id: string,
    discussion_number: string,
    comment_number: string,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: getByTeam_idAndDiscussion_numberAndComment_number.path(
        team_id,
        discussion_number,
        comment_number,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("team_id")(() => typia.assert(team_id));
      assert.param("discussion_number")(() => typia.assert(discussion_number));
      assert.param("comment_number")(() => typia.assert(comment_number));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Update a discussion comment (Legacy).
 * > [!WARNING]
 * > **Deprecation notice:** This endpoint route is deprecated and will be removed from the Teams API. We recommend migrating your existing code to use the new [Update a discussion comment](https://docs.github.com/rest/teams/discussion-comments#update-a-discussion-comment) endpoint.
 *
 * Edits the body text of a discussion comment.
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:discussion` scope to use this endpoint.
 *
 * @tag teams
 * @deprecated
 * @path teams/:team_id/discussions/:discussion_number/comments/:comment_number
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function patchByTeam_idAndDiscussion_numberAndComment_number(
  connection: IConnection<patchByTeam_idAndDiscussion_numberAndComment_number.Headers>,
  team_id: string,
  discussion_number: string,
  comment_number: string,
  body: patchByTeam_idAndDiscussion_numberAndComment_number.Input,
): Promise<patchByTeam_idAndDiscussion_numberAndComment_number.Output> {
  return !!connection.simulate
    ? patchByTeam_idAndDiscussion_numberAndComment_number.simulate(
        connection,
        team_id,
        discussion_number,
        comment_number,
        body,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...patchByTeam_idAndDiscussion_numberAndComment_number.METADATA,
          path: patchByTeam_idAndDiscussion_numberAndComment_number.path(
            team_id,
            discussion_number,
            comment_number,
          ),
          status: null,
        },
        body,
      );
}
export namespace patchByTeam_idAndDiscussion_numberAndComment_number {
  export type Headers = IApiTeamsDiscussionsComments.PatchHeader;
  export type Input = body;
  export type Output = team_minus_discussion_minus_comment;

  export const METADATA = {
    method: "PATCH",
    path: "/teams/:team_id/discussions/:discussion_number/comments/:comment_number",
    request: {
      type: "application/json",
      encrypted: false,
    },
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    team_id: string,
    discussion_number: string,
    comment_number: string,
  ) =>
    `/teams/${encodeURIComponent(team_id ?? "null")}/discussions/${encodeURIComponent(discussion_number ?? "null")}/comments/${encodeURIComponent(comment_number ?? "null")}`;
  export const random = (
    g?: Partial<typia.IRandomGenerator>,
  ): team_minus_discussion_minus_comment =>
    typia.random<team_minus_discussion_minus_comment>(g);
  export const simulate = (
    connection: IConnection<patchByTeam_idAndDiscussion_numberAndComment_number.Headers>,
    team_id: string,
    discussion_number: string,
    comment_number: string,
    body: patchByTeam_idAndDiscussion_numberAndComment_number.Input,
  ): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: patchByTeam_idAndDiscussion_numberAndComment_number.path(
        team_id,
        discussion_number,
        comment_number,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("team_id")(() => typia.assert(team_id));
      assert.param("discussion_number")(() => typia.assert(discussion_number));
      assert.param("comment_number")(() => typia.assert(comment_number));
      assert.body(() => typia.assert(body));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
/**
 * Delete a discussion comment (Legacy).
 * > [!WARNING]
 * > **Deprecation notice:** This endpoint route is deprecated and will be removed from the Teams API. We recommend migrating your existing code to use the new [Delete a discussion comment](https://docs.github.com/rest/teams/discussion-comments#delete-a-discussion-comment) endpoint.
 *
 * Deletes a comment on a team discussion.
 *
 * OAuth app tokens and personal access tokens (classic) need the `write:discussion` scope to use this endpoint.
 *
 * @tag teams
 * @deprecated
 * @path teams/:team_id/discussions/:discussion_number/comments/:comment_number
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function eraseByTeam_idAndDiscussion_numberAndComment_number(
  connection: IConnection<eraseByTeam_idAndDiscussion_numberAndComment_number.Headers>,
  team_id: string,
  discussion_number: string,
  comment_number: string,
): Promise<void> {
  return !!connection.simulate
    ? eraseByTeam_idAndDiscussion_numberAndComment_number.simulate(
        connection,
        team_id,
        discussion_number,
        comment_number,
      )
    : PlainFetcher.fetch(
        {
          ...connection,
          headers: {
            ...connection.headers,
            "Content-Type": "application/json",
          },
        },
        {
          ...eraseByTeam_idAndDiscussion_numberAndComment_number.METADATA,
          path: eraseByTeam_idAndDiscussion_numberAndComment_number.path(
            team_id,
            discussion_number,
            comment_number,
          ),
          status: null,
        },
      );
}
export namespace eraseByTeam_idAndDiscussion_numberAndComment_number {
  export type Headers = IApiTeamsDiscussionsComments.DeleteHeader;

  export const METADATA = {
    method: "DELETE",
    path: "/teams/:team_id/discussions/:discussion_number/comments/:comment_number",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
  } as const;

  export const path = (
    team_id: string,
    discussion_number: string,
    comment_number: string,
  ) =>
    `/teams/${encodeURIComponent(team_id ?? "null")}/discussions/${encodeURIComponent(discussion_number ?? "null")}/comments/${encodeURIComponent(comment_number ?? "null")}`;
  export const random = (g?: Partial<typia.IRandomGenerator>): void =>
    typia.random<void>(g);
  export const simulate = (
    connection: IConnection<eraseByTeam_idAndDiscussion_numberAndComment_number.Headers>,
    team_id: string,
    discussion_number: string,
    comment_number: string,
  ): void => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: eraseByTeam_idAndDiscussion_numberAndComment_number.path(
        team_id,
        discussion_number,
        comment_number,
      ),
      contentType: "application/json",
    });
    try {
      assert.param("team_id")(() => typia.assert(team_id));
      assert.param("discussion_number")(() => typia.assert(discussion_number));
      assert.param("comment_number")(() => typia.assert(comment_number));
    } catch (exp) {
      if (!typia.is<HttpError>(exp)) throw exp;
      return {
        success: false,
        status: exp.status,
        headers: exp.headers,
        data: exp.toJSON().message,
      } as any;
    }
    return random(
      "object" === typeof connection.simulate && null !== connection.simulate
        ? connection.simulate
        : undefined,
    );
  };
}
