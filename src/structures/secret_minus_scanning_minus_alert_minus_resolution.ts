/**
 * **Required when the `state` is `resolved`.** The reason for resolving the alert.
 */
export type secret_minus_scanning_minus_alert_minus_resolution =
  | "false_positive"
  | "wont_fix"
  | "revoked"
  | "used_in_tests"
  | null;
