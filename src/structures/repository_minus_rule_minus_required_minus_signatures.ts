/**
 * Commits pushed to matching refs must have verified signatures.
 *
 * @title required_signatures
 */
export type repository_minus_rule_minus_required_minus_signatures = {
  type: "required_signatures";
};
