/**
 * An optional comment when closing an alert. Cannot be updated or deleted. Must be `null` when changing `state` to `open`.
 */
export type secret_minus_scanning_minus_alert_minus_resolution_minus_comment =
  | string
  | null;
