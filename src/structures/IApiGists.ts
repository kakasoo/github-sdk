export namespace IApiGists {
  export type GetHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
  export type PatchHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
  export type DeleteHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
}
