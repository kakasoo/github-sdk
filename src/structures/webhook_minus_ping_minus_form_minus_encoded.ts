/**
 * The webhooks ping payload encoded with URL encoding.
 */
export type webhook_minus_ping_minus_form_minus_encoded = {
  /**
   * A URL-encoded string of the ping JSON payload. The decoded payload is a JSON object.
   */
  payload: string;
};
