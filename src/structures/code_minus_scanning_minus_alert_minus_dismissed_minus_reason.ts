/**
 * **Required when the state is dismissed.** The reason for dismissing or closing the alert.
 */
export type code_minus_scanning_minus_alert_minus_dismissed_minus_reason =
  | "false positive"
  | "won't fix"
  | "used in tests"
  | null;
