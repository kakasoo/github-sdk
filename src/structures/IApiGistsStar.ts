export namespace IApiGistsStar {
  export type GetHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
  export type PutHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
  export type DeleteHeader = {
    /**
     * The unique identifier of the gist.
     */
    gist_id: string;
  };
}
